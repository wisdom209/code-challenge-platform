Turn-in directory: c09/ex02/

Files to turn in: ft_split.c

Allowed functions: malloc

Required files: ft_split.c

Prototype: char **ft_split(char *str, char *charset);

Description: Split `str` using any character from `charset` as separator. Return a NULL‑terminated array of strings (no empty strings). The input string is read‑only. Memory must be allocated with `malloc`.
