Turn-in directory: c09/ex01/

Files to turn in: Makefile.c

Allowed functions: none specified

Required files: Makefile

Prototype:

Description: Create a `Makefile` that compiles `libft.a` from source files in `srcs/` (ft_putchar.c, ft_swap.c, ft_putstr.c, ft_strlen.c, ft_strcmp.c) and header in `includes/ft.h`. It must implement rules: `all`, `clean`, `fclean`, `re`, `libft.a`. Use `-Wall -Wextra -Werror`. No unnecessary recompilation.
