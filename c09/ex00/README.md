Turn-in directory: c09/ex00/

Files to turn in: libft_creator.c

Allowed functions: write

Required files: libft_creator.sh, ft_putchar.c, ft_swap.c, ft_putstr.c, ft_strlen.c, ft_strcmp.c

Prototype:

Description: Create a shell script `libft_creator.sh` that compiles the provided .c files and creates `libft.a` (a static library). The script must run with `sh libft_creator.sh` and produce `libft.a` in the same directory.
